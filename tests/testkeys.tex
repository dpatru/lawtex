\documentclass{article}
\usepackage{pgfkeys}
\begin{document}
% \def\assert#1#2#3{%
%   \pgfkeys{/assert/.cd, testname=#1, a=#2, b=#3}}

\def\usingdef#1+#2XXX{\def\a{#1}\def\b{#2} result\par}
calling usingdef: \usingdef usingdef at outer layer+hello worldXXX\par
\verb|\a| is \a, \verb|\b| is \b. 

% raises errors
% calling mybad from edef: 
% \edef\bad{ In bad\par\usingdef from+badXXX}\bad 
% \verb|\a| is \a, \verb|\b| is \b. 


\def\mystore#1+#2{\def\a{#1}\def\b{#2} result\par}
\def\mystore#1+#2\pgfeov{\def\a{#1}\def\b{#2} result\par}
% \pgfkeyslet{/my key/.@cmd}{\mystore}

  \pgfkeyslet{/my key/.@cmd}{\mystore}%

% no errors 
calling good from edef:
\edef\good{% edef throws error. 
  In good\par%
  \pgfkeys{/my key=from+good}\par Good out\par%
}
Calling good: \good\par
\verb|\a| is \a, \verb|\b| is \b. 


% \pgfkeyslet{/my key/.@cmd}{\mystore}
% \pgfkeys{/my key=hello+world}
% \verb|\a| is \a, \verb|\b| is \b. 


\end{document}
